---
npm/@k-msg/cli: patch
---

Upgrade the CLI to the Bunli 0.9.1 toolchain, preserve strict boolean and empty-string argument handling, and add fullscreen interactive TUI flows for `k-msg config init` and `k-msg config provider add`.
