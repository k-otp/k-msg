---
npm/@k-msg/provider: patch
---

Broaden SOLAPI peer compatibility to cover the v6 SDK line, prefer the newer `send()` path when available while keeping v5 `sendOne()` compatibility, and refresh install guidance to recommend the latest `solapi` package.
