---
npm/@k-msg/provider: patch
---

Refactor built-in providers (`iwinv`, `solapi`, `aligo`) into facade + domain modules while keeping public APIs unchanged.
Also adds a reusable shared base64 utility and provider structure guide for consistent future provider splits.
